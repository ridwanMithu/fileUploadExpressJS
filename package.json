{
  "name": "day-38",
  "version": "1.0.0",
  "description": "Todays topic covers the process of importing images from backend server so that we do not have to host our images in a database directly rather we can use an url",
  "main": "index.js",
  "type module": "convert type into module so that we can use es6 commands for the index.js file instead of traditional commands we also have to replace requires with imports",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "nodemon index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "busboy": "^1.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.2",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1"
  }
}
